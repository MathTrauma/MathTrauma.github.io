{
    "name": "my-blog",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "node render.mjs",
        "build:force": "node render.mjs --force",
        "watch": "nodemon --watch posts --watch templates --ext md,html,css render.mjs",
        "serve": "npx serve dist"
    },
    "dependencies": {
        "marked": "^14.0.0"
    },
    "devDependencies": {
        "nodemon": "^3.1.11"
    }
}
